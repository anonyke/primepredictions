server {
    listen 80;
    server_name www.pripredict.co.ke pripredict.co.ke;

    # Redirect all HTTP to HTTPS (Certbot will update to 443 blocks)
    return 301 https://$host$request_uri;
}

# HTTPS server block (template) - certbot will generate real certificates
server {
    listen 443 ssl;
    server_name www.pripredict.co.ke pripredict.co.ke;

    # SSL cert paths (Certbot will populate these)
    ssl_certificate /etc/letsencrypt/live/www.pripredict.co.ke/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/www.pripredict.co.ke/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Frontend proxy
    # Serve frontend (Next.js) via proxy
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API proxy
    location /api/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Security headers
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
}
